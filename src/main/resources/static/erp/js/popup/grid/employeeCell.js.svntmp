function EmployeeCell(targetGrid, name) {
	CellPopupGrid.call(this, targetGrid, name, {
		imageUrl : imageUrl,
		width : 650,
		height : 300,
		xml : 'xml/popup/employee/grid.xml'
	});

	this.setUrlPrefix('popup/employee');
}

EmployeeCell.prototype = Object.create(CellPopupGrid.prototype);
EmployeeCell.prototype.constructor = EmployeeCell;
